import React from 'react';

const Card = ({logement}) => {
    return (
        <div>
            <ul>
                {

                }
            </ul>
        </div>
    );
};

export default Card;